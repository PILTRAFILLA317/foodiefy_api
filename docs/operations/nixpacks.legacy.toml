[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python3 -m venv /app/.venv", "/app/.venv/bin/pip install -r requirements.lock"]

[start]
cmd = "/app/.venv/bin/uvicorn src.fastapi_app:create_app --factory --host 0.0.0.0 --port ${PORT:-8080}"
